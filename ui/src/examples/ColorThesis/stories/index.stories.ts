import type { Meta, StoryObj } from '@storybook/react'

import { ColorThesis } from '../index'

const meta: Meta<typeof ColorThesis> = {
  component: ColorThesis,
}

export default meta
type Story = StoryObj<typeof ColorThesis>

export const Example: Story = {}
